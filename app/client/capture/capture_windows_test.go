package capture

import (
	"testing"
)

func Test(t *testing.T) {

	// t.Run("wsl", func(t *testing.T) {
	// 	{
	// 		addr, nic, err := divert.Gateway(netip.AddrFrom4([4]byte{192, 168, 16, 1}))
	// 		fmt.Println(addr.String())
	// 		fmt.Println(nic)
	// 		fmt.Println(err)
	// 	}
	// 	fmt.Println()
	// 	{
	// 		addr, nic, err := divert.Gateway(netip.AddrFrom4([4]byte{192, 168, 21, 146}))
	// 		fmt.Println(addr.String())
	// 		fmt.Println(nic)
	// 		fmt.Println(err)
	// 	}
	// })

	// t.Run("default", func(t *testing.T) {
	// 	{
	// 		addr, nic, err := divert.Gateway(netip.AddrFrom4([4]byte{0, 0, 0, 0}))
	// 		fmt.Println(addr.String())
	// 		fmt.Println(nic)
	// 		fmt.Println(err)
	// 	}
	// 	fmt.Println()
	// 	{
	// 		addr, nic, err := divert.Gateway(netip.AddrFrom4([4]byte{8, 8, 8, 8}))
	// 		fmt.Println(addr.String())
	// 		fmt.Println(nic)
	// 		fmt.Println(err)
	// 	}
	// })
}
